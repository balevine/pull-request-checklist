name: Checklist

on: pull_request
jobs:
  create-checklist-comment:
    name: checklist comment
    runs-on: ubuntu-latest
    steps:
      - name: create comment
        uses: balevine/actions/pr-checklist@production
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
